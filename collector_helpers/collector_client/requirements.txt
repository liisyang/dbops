# DBOPS Collector Client — Execution Node Dependencies
# ====================================================
# Install on AWX Execution Node:
#   /opt/dbops-collector/venv/bin/pip install -r collector_helpers/collector_client/requirements.txt
#
# System dependencies (apt/yum):
#   unixodbc unixodbc-dev          (for SQL Server pyodbc)
#   Microsoft ODBC Driver 17/18 for SQL Server
#     https://learn.microsoft.com/en-us/sql/connect/odbc/linux-mac/installing-the-microsoft-odbc-driver-for-sql-server

# Core
pydantic>=2.0

# Oracle — thin mode, no Instant Client needed
oracledb>=1.4

# SQL Server — requires unixODBC + Microsoft ODBC Driver 17/18
pyodbc>=4.0.39

# PostgreSQL — psycopg 3
psycopg[binary]>=3.0

# MySQL — STUB ONLY in Phase 3.3A
pymysql>=1.0

# Linux SSH
paramiko>=3.0
